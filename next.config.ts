import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "via.placeholder.com",
      "www.tatvasoft.com", 
      "strapi.dhiwise.com",
      "images.prismic.io",
      "miro.medium.com", 
      "www.syncfusion.com",
      "askgalore.com", 
    ],
  },
};

export default nextConfig;
